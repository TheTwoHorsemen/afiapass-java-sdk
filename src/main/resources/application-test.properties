
#=====================================================
#  Configuring hibernate to use double quote       =
#  the Table to all lowercase in naming the table  =
#=====================================================

# H2 in-memory database
spring.datasource.url= ${H2_DB_URL}
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.show-sql=true

spring.datasource.username= ${H2_DB_USERNAME}
spring.datasource.password= ${H2_DB_PASSWORD}

# Let Hibernate build schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Optional: enable H2 console during tests
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console






